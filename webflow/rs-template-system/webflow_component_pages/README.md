# Webflow Component Pages

Copied output generated from `../master_ks`. Do not treat this folder as gold source.

- `pages/` contains local browser preview pages, one file per component.
- `assets/` contains copied `rs-global.css` and `rs-scripts.js` for local preview.

For Webflow production use, use the existing project-specific Webflow embed workflow. This folder is only for page/component preview and section selection.
