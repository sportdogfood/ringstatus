{
  "scripts": {
    "build": "cd webflow-cloud-test && npm install && npm run build && cd .. && if (Test-Path dist) { Remove-Item dist -Recurse -Force } && Copy-Item webflow-cloud-test\\dist dist -Recurse"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {}
}